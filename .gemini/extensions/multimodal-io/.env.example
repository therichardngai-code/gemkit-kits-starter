# Gemini API Key (required)
# Get from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-api-key-here

# .env file search order (first found wins):
# 1. .gemini/extensions/multimodal-io/.env (this file)
# 2. Project root .env (searches up to git root)
# 3. ~/.gemini/.env, ~/.gemini.env, ~/.env
#
# Note: Shell environment variables (export GEMINI_API_KEY=...)
# always take precedence over .env files.
